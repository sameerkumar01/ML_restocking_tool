[project]
name = "mobile-inventory-restocking-tool"
version = "2.0.0"
description = "Schema-adaptive inventory recommendation and demand forecasting application"
requires-python = ">=3.11,<3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
